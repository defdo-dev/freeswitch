<!--
  Enhanced Erlang Event Configuration
  Supports adaptive protocol negotiation for OTP 21 through 27+
  
  Joe Armstrong's approach: "Let it crash, but gracefully!"
-->
<configuration name="erlang_event.conf" description="Enhanced Erlang Socket Client">
  <settings>
    <!-- Basic connection settings -->
    <param name="listen-ip" value="0.0.0.0"/>
    <param name="listen-port" value="8031"/>
    <!-- Specify the first part of the node name
         (the host part after the @ will be autodetected)
         OR pass a complete nodename to avoid autodetection
         eg. freeswitch@example or freeswitch@example.com.
         If you pass a complete node name, the 'shortname' parameter has no effect. -->
    <param name="nodename" value="freeswitch"/>
    <!-- Specify this OR 'cookie-file' or $HOME/.erlang.cookie will be read -->
    <param name="cookie" value="ClueCon"/>
    <!-- Read a cookie from an arbitary erlang cookie file instead -->
    <!--<param name="cookie-file" value="/tmp/erlang.cookie"/>-->
    <param name="shortname" value="true"/>
    
    <!-- Enhanced Protocol Settings (NEW) -->
    <!-- Enable adaptive protocol negotiation (recommended for OTP 25+) -->
    <param name="adaptive-protocol" value="true"/>
    
    <!-- Force legacy compatibility mode for all connections -->
    <!-- Use only if experiencing issues with modern Erlang nodes -->
    <param name="force-compat-mode" value="false"/>
    
    <!-- Timeout for protocol detection in milliseconds -->
    <param name="protocol-timeout" value="5000"/>
    
    <!-- Enable detailed protocol negotiation logging -->
    <param name="debug-protocol" value="false"/>
    
    <!-- Legacy compatibility release (deprecated) -->
    <!-- Only use if adaptive-protocol is disabled -->
    <!-- <param name="compat-rel" value="21"/> -->
    
    <!-- Security settings -->
    <param name="apply-inbound-acl" value="lan"/>
    
    <!-- Encoding settings -->
    <param name="encoding" value="string"/>
    
    <!-- Performance tuning -->
    <param name="max-event-bulk" value="500"/>
    <param name="max-log-bulk" value="100"/>
    
    <!-- Error handling -->
    <param name="stop-on-bind-error" value="false"/>
  </settings>
</configuration>
